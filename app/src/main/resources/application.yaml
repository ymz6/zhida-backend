spring:
  application:
    name: zhida
  servlet:
    multipart:
      # 全局上传保护阈值，具体业务限制在各自 Service 中校验
      max-file-size: 10MB
      max-request-size: 10MB
logging:
  level:
    "[org.ymz.app]": DEBUG
mybatis-flex:
  # Mapper XML 文件位置
  mapper-locations: classpath*:/mapper/**/*.xml