<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ymz.app.mapper.CommentMapper">

    <select id="selectCommentById" resultType="org.ymz.app.model.entity.Comment">
        select *
        from comment
        where id = #{commentId}
        limit 1
    </select>

    <select id="listByIds" resultType="org.ymz.app.model.entity.Comment">
        select *
        from comment
        where id in
        <foreach collection="commentIds" item="commentId" open="(" separator="," close=")">
            #{commentId}
        </foreach>
    </select>

    <select id="paginateRootComments" resultType="org.ymz.app.model.entity.Comment">
        select *
        from comment
        where app_id = #{appId}
          and root_id is null
        order by created_at desc
    </select>

    <select id="paginateReplies" resultType="org.ymz.app.model.entity.Comment">
        select *
        from comment
        where root_id = #{rootId}
        order by created_at asc
    </select>

    <select id="countRepliesByRootIds" resultType="org.ymz.app.model.dto.comment.CommentCountItem">
        select root_id as commentId,
               count(1) as count
        from comment
        where root_id in
        <foreach collection="rootIds" item="rootId" open="(" separator="," close=")">
            #{rootId}
        </foreach>
        group by root_id
    </select>

    <select id="listSubtreeIds" resultType="long">
        with recursive comment_tree as (
            select id
            from comment
            where id = #{commentId}
            union all
            select c.id
            from comment c
                     join comment_tree ct on c.parent_id = ct.id
        )
        select id
        from comment_tree
    </select>

    <delete id="deleteByIds">
        delete from comment
        where id in
        <foreach collection="commentIds" item="commentId" open="(" separator="," close=")">
            #{commentId}
        </foreach>
    </delete>
</mapper>
